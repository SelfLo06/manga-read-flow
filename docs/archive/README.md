# Archived Documents

This directory stores superseded top-level planning and baseline drafts.

Current authoritative documents remain in the normal `docs/` paths. Use:

- `docs/SRS-v1.0.md` for the current requirements baseline.
- `docs/HLD.md` for the current architecture baseline.
- `docs/PROJECT-PLAN.md` for the current process and milestone plan.

Archived files are retained for traceability only and should not be used as current inputs.
