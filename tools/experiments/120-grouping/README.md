# 120 Grouping 实验工具

`text_seeded_container_association/` 保留仍被测试使用的 association、routing、calibration 与 evaluator。其历史 run 已迁入 `data/local/runs` 或 `data/local/reviews`；代码中的默认输出也必须指向这些本地目录。

这些工具只复现实验能力，不代表自动 Grouping、mask 或 Cleaning 已获产品授权。
